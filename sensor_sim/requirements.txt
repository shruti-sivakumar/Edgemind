# sensor_sim/requirements.txt
#
# Person A (fault_engine.py, pump_config.py) — standard library only.
# Person B (opc_server.py, main.py)          — adds asyncua + uvicorn below.
# Person C (inject_server.py)                — adds fastapi below.
#
# All three: pytest for tests.

# ── Testing (all persons) ─────────────────────────────────────────────────────
pytest>=7.0
pytest-asyncio>=0.24.0

# ── Person B: OPC-UA server ───────────────────────────────────────────────────
asyncua==1.1.5

# ── Person B/C: ASGI + HTTP server ───────────────────────────────────────────
uvicorn[standard]==0.32.1

# ── Person C: FastAPI inject endpoint ────────────────────────────────────────
fastapi==0.115.5
pydantic>=2.0
httpx>=0.27.0
